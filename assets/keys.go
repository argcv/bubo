package assets

const (
	KeyServer = "server"
	KeyClient = "client"

	KeyRPCSrvBind = KeyServer + ".bind"
	KeyRPCSrvPort = KeyServer + ".port"
	KeyRPCCliHost = KeyClient + ".host"
	KeyRPCCliPort = KeyClient + ".port"
)
